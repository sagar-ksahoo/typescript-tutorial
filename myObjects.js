"use strict";
exports.__esModule = true;
var User = {
    name: "sagar",
    email: "sagar@gmail.com",
    isActive: true
};
function createUser(_a) {
    var string = _a.name, boolean = _a.isAuth;
}
createUser({ name: "sagar", isAuth: true });
function createCourse() {
    return { name: "javascript", price: 499 };
}
